import { HeaderContainer } from "./Header.styles"
export function Header() {
  return (
    <HeaderContainer>
      <div>
        <nav>
          <a>RECYCLE</a>
        </nav>
        <button>
          DDS
        </button>
      </div>
    </HeaderContainer>
  )
}